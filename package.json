{
  "name": "easter-hunt",
  "version": "2.0.0",
  "description": "Easter Egg Hunt — Netlify + Supabase multiplayer & single-player trivia + clue game",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step — vanilla JS with CDN'"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "dependencies": {
    "@netlify/functions": "^2.6.0",
    "@supabase/supabase-js": "^2.43.4"
  }
}
